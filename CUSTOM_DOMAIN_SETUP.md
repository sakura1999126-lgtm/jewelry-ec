# 🔗 カスタムドメイン設定ガイド

## 現在のURL
`https://jewelry-ec.sakura1114jf.workers.dev/`

## 目標のURLオプション

### オプション1: Cloudflare Pagesの標準ドメイン（推奨・無料）
`https://jewelry-ec.pages.dev/`

### オプション2: カスタムドメイン
お持ちのドメインがあれば、それを設定できます。

---

## 方法1: Cloudflare Pagesの標準ドメインを使用

### 現在の状況
プロジェクトが「Workers」として作成されているため、`*.workers.dev`のドメインが割り当てられています。

### 解決方法
1. **Cloudflareダッシュボードを開く**
   - https://dash.cloudflare.com/

2. **左サイドバーで「Pages」を探す**
   - 「Workers & Pages」の下にある「Pages」をクリック

3. **新しいプロジェクトを作成**
   - 「プロジェクトを作成」をクリック
   - 「Gitに接続」を選択
   - GitHubリポジトリ「jewelry-ec」を選択

4. **設定（すべて空白）**
   - フレームワークプリセット: 空白
   - ビルドコマンド: 空白
   - ビルド出力ディレクトリ: 空白
   - ルートディレクトリ: 空白

5. **「保存してデプロイ」をクリック**

6. **デプロイが完了すると、以下のようなURLが表示されます：**
   ```
   https://jewelry-ec.pages.dev
   ```
   これが標準のPagesドメインです（`sakura1114jf`は含まれません）

---

## 方法2: カスタムドメインを設定（ドメインをお持ちの場合）

1. **Cloudflare Pagesダッシュボードでプロジェクトを開く**

2. **「カスタムドメイン」タブを開く**

3. **「ドメインを設定」をクリック**

4. **お持ちのドメインを入力**
   - 例: `yourdomain.com` または `jewelry.yourdomain.com`

5. **DNS設定を確認**
   - Cloudflareが自動でDNSレコードを追加します

---

## ⚠️ 注意事項

- 現在のWorkersプロジェクトを削除する必要はありません（両方存在できます）
- Pagesプロジェクトとして作成すると、`*.pages.dev`のドメインが自動で割り当てられます
- カスタムドメインを設定する場合は、そのドメインをCloudflareで管理している必要があります

---

## ✅ 推奨される手順

**方法1（Pages標準ドメイン）** を推奨します：
- ✅ 無料
- ✅ すぐに使用可能
- ✅ `sakura1114jf`が含まれない
- ✅ 設定が簡単

