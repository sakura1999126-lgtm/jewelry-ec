# 🔧 Cloudflare Pages 静的サイトデプロイ修正（日本語版）

## 問題
Cloudflare Pagesが`wrangler`コマンドを実行しようとしてエラーが発生しています。
静的サイトとしてデプロイする必要があります。

---

## ✅ 解決方法

### 方法1: Cloudflare Pagesダッシュボードで設定を変更

1. **プロジェクトの「設定」タブを開く**

2. **「ビルドとデプロイ」セクションを開く**

3. **以下を確認・設定**:
   - **フレームワークプリセット**: `なし`（または空白）
   - **ビルドコマンド**: （**完全に空白**）
   - **ビルド出力ディレクトリ**: （**完全に空白**）
   - **ルートディレクトリ**: （**完全に空白**）
   - **⚠️ デプロイコマンド**: （**完全に空白にする - 重要！**）

4. **「保存」をクリック**

5. **「再デプロイ」ボタンをクリック**（または「概要」タブから「Retry deployment」をクリック）

---

### 方法2: プロジェクトを再作成（方法1で解決しない場合）

1. **新しいプロジェクトを作成**:
   - 「プロジェクトを作成」→「Gitに接続」
   - GitHubリポジトリ「jewelry-ec」を選択

2. **設定時**:
   - フレームワークプリセット: **選択しない（空白）**
   - ビルドコマンド: **空白**
   - ビルド出力ディレクトリ: **空白**
   - ルートディレクトリ: **空白**

3. **「セットアップを開始」→「保存してデプロイ」**

---

## 📁 静的サイトとして必要なファイル

以下がルートディレクトリに配置されていることを確認：

- ✅ `index.html`
- ✅ `checkout.html`
- ✅ `script.js`
- ✅ `styles.css`
- ✅ `products.json`
- ✅ `_redirects`（SPA用リダイレクト）
- ✅ `images/`フォルダ（`public/images/`の中身）
- ✅ `videos/`フォルダ（`public/videos/`の中身）

---

## ⚠️ 不要なファイル

以下は静的サイトデプロイには不要（存在しても問題ありませんが）：

- `server.js`（静的サイトでは使用されない）
- `package.json`（静的サイトでは使用されない）
- `vercel.json`（Vercel用、Cloudflareでは無視されます）

---

## ✅ 正しい設定確認

Cloudflare Pagesダッシュボードで確認：

1. **「概要」タブ**で最新のビルドを確認
2. **ビルドステップ**が以下になっているはず：
   - ✓ ツールと依存関係のインストール
   - ✓ アプリケーションをビルドしています（スキップされるか、すぐ終了）
   - ✓ Cloudflare のグローバルネットワークにデプロイしています

3. **エラーが出ないことを確認**

---

## 🚀 デプロイが成功したら

以下のようなURLが表示されます：
```
https://jewelry-ec.pages.dev
```

このURLを友達にシェアできます！

---

## 📝 日本語版の画面で確認する場所

- **設定タブ**: 左サイドバーの「設定」をクリック
- **ビルドとデプロイ**: 設定ページ内の「ビルドとデプロイ」セクション
- **再デプロイ**: 概要タブのビルド履歴から「再デプロイ」ボタンをクリック

